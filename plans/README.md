# Implementation Plans

Round 2 generated by the improve skill on **2026-07-21** against commit `ceed480` (branch `main`). Round 1 (2026-07-08, commit `baebbd7`) produced plans 001–006 — all verified landed on 2026-07-21 with no regressions (optimize billing guarantees re-checked after #123; `safe-fetch` SSRF guard re-verified clean; root artifacts untracked; `parse-arabic-resume.ts` deleted).

Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Selection note (round 2): the owner selected all four offered clusters plus the usage-attribution direction spike. Unselected findings are recorded under "candidates" below.

> **2026-07-21 — `/improve security` round (against commit `ceed480`).** A focused security re-audit (three parallel read-only auditors: auth/authz, injection/SSRF, secrets/config/supply-chain) added plans **007–012** below. Headline: the money/credit path, SSRF guard, Puppeteer PDF sandbox, RLS, and secret hygiene are all solid — no HIGH-severity findings survived vetting. The new plans close MED/LOW hardening gaps. Ran non-interactively, so plans were written for the vetted findings by leverage; unplanned items are in the findings section below. **Correction during review:** the audit's highest-rated item (SEC-07, cron-gate) was **rejected after verifying a Netlify platform fact** — scheduled functions are not publicly URL-invocable, so the header-only gate is safe and requiring `CRON_SECRET` would have broken the crons. Plan 007 was narrowed to the constant-time admin-secret fix (SEC-08) only. Details below.

> **2026-08-08 — Round 3 `/improve` (against commit `d2fba38`).** All of rounds 1–2 (plans 001–016, both numberings) are DONE, so this round scoped itself to the **283 files / +21.8k lines added since `ceed480`** plus the carryover candidates that were gated on now-completed plans. Four parallel read-only auditors (backend/money/migrations, frontend, eval subsystem + test integrity, deps/DX/docs/direction) plus a direct pass on the four `fix:` repair commits. Added plans **017–023**, numbered to continue past 016 so no third numbering collision exists. Baseline at planning time: `npm run type:check` exits 0, `npm run test` exits 0 (181 files, 1774 passed, 2 skipped). Headline: the credit RPC lost two input guards in a July repair migration, three authenticated endpoints have no rate limiting at all, and both gold-set eval harnesses can report success without measuring the provider they claim to.
>
> **Tooling warning for future rounds:** shell `grep` (via the rtk proxy) returned **false negatives** on this repo during this audit — it reported zero matches for `it.skip` when two exist. Any "X does not exist anywhere" claim must be verified with the Grep tool, not a shell pipeline. A deletion or removal decision based on a shell-grep negative is not safe.

## Execution order & status

> **Numbering collision (read this first).** Two independent rounds were planned in parallel against the same commit `ceed480` and both numbered their plans from 007. The plan *files* do not collide (the names differ), but the numbers do. Always refer to a plan by its **filename**, not its number. Round 2 is `007-restore-credit-init-anti-abuse.md` … `016-ai-usage-user-attribution-spike.md`; the security round is `007-enforce-cron-secret-and-constant-time-admin.md` … `012-rate-limit-batch-api-fanout.md`.

### Round 1 (2026-07-08)

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Enforce credit consumption results in optimize endpoints | P1 | S | — | DONE |
| 002 | Unify optimize cache key; stop serving cached credit balances | P1 | M | 001 | DONE |
| 003 | Billing-safe stream error events (no double-charge fallback) | P1 | S–M | 001, 002 | DONE |
| 004 | Atomic referral completion (claim-then-award) | P2 | S | — | DONE |
| 005 | Harden abuse-facing surfaces (cron gate, beta gate, dead fn, email escaping) | P2 | M | — | DONE |
| 006 | Toolchain & repo hygiene (dead dep, tracked artifacts, honest gate, env docs) | P2 | S | — | DONE |

### Round 2 — general improve round (2026-07-21)

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 007 | Restore signup anti-abuse checks; atomic addCredits | P1 | M | — | DONE |
| 008 | Pay referral rewards on first paid action | P1 | M | 007 | DONE |
| 009 | CI pipeline gating main (lint/typecheck/test) | P1 | S | — | DONE |
| 010 | Handler tests for six untested AI functions | P2 | M | 009 (soft) | DONE |
| 011 | Extract shared optimize card/score lib | P2 | M | — | DONE |
| 012 | OptimizeSection render hygiene (selectors/useCallback/memo) | P2 | M | — | DONE |
| 013 | Mixpanel deferral + client error-envelope hygiene + vision2030 credit guard | P2 | S–M | 010 (part C only) | DONE |
| 014 | Gate parked mascot CSS; right-size character images | P3 | S | — | DONE (B2 skipped: no approved local image encoder) |
| 015 | Docs & DX hygiene (README setup, env docs, CLAUDE.md de-drift, spec archive) | P3 | S | — | DONE |
| 016 | SPIKE: user attribution on ai_usage_events (unblocks job-variant ADR) | P2 | S–M | — | DONE |

### Security round (2026-07-21) — separate numbering, see collision note above

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 007 (sec) | Constant-time admin-secret comparison (SEC-08) | P3 | S | — | DONE |
| 008 (sec) | Add report-only CSP + framing/transport headers | P2 | M | — | DONE |
| 009 (sec) | Scrub PII from client-side Sentry error events | P2 | S | — | DONE |
| 010 (sec) | Extract + behaviorally test the generate-pdf sandbox controls | P2 | M | — | DONE |
| 011 (sec) | Close empty-Content-Type gap in safe-fetch + bound job-extract regex | P3 | S | — | DONE |
| 012 (sec) | Rate-limit the batch-api fan-out endpoint | P3 | S | — | DONE |

### Round 3 (2026-08-08) — numbering continues past 016, no collision

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 017 | Restore the input guards `add_credits` lost; make credit migrations self-valid | P1 | S | — | DONE (reviewed, APPROVE) — commit `8b5f053` on `advisor/017-restore-add-credits-guards`, **not merged** |
| 018 | Rate-limit `feedback-api`, `referral-api`, `user-data-api` | P1 | S | — | DONE (reviewed, APPROVE) — commit `467c57d` on `advisor/018-rate-limit-credit-endpoints`, **not merged** |
| 019 | Fix InterviewSection practice-answer key collision | P1 | S | — | DONE (reviewed, APPROVE after 1 revision) — commits `2f53d20`+`210b26e` on `advisor/019-interview-answer-key-collision`, **not merged** |
| 020 | Make the eval harnesses fail closed (fallback, provider, missing cache) | P1 | S | — | DONE (reviewed, APPROVE) — commit `47fb5c4` on `advisor/020-fail-closed-eval-harnesses`, **not merged** |
| 021 | Fix the server PDF fallback's detached-ArrayBuffer crash | P2 | S | — | DONE (reviewed, APPROVE) — commit `e5808f0` on `advisor/021-server-pdf-detached-buffer`, **not merged** |
| 022 | SPIKE: instrument the ADR's Phase-2 gate for Job Variants | P2 | S | — | DONE (reviewed, APPROVE) — commit `41e82fa` on `advisor/022-spike-job-variant-gate`, **not merged** |
| 023 | SPIKE: decide whether `batch-api` is a product surface or dead weight | P3 | S | — | DONE (reviewed, APPROVE) — deliverable at `plans/023-outcome-batch-api-decision.md`; **uncommitted**, see execution log |
| 024 | Split vitest into `dom` / `node` projects (stop happy-dom for 55 node-only files) | P2 | S | — | DONE (reviewed, APPROVE) — commit `6f5ae73`, **MERGED** as `e0c0a9b` |

**024 outcome — the flakiness mitigation worked, measured not assumed.** Written and executed after plans 017–022 were merged, in response to the suite failing 2 runs in 3 on the merged tree.

| | Pre-split | Post-split |
|---|---|---|
| Full suites green | **1 of 3** | **3 of 3** |
| Wall clock | 405 / 418 / 594 s | 283 / 299 / 352 s |
| `environment` phase | 519 / 571 / 799 s | 327 / 337 / 400 s |
| Tests | 1794 passed / 2 skipped | 1794 passed / 2 skipped (**identical**) |

The `node` project alone: 56 files, 727 tests, 86s, **`environment 70ms`** — that overhead is simply gone. Equivalence proven by a sorted `diff` of `vitest list --filesOnly` before/after: **183 = 183, empty diff**, so no directory silently stopped being collected and nothing is double-collected. Per-project sums reconcile: 56 + 127 = 183 files, 727 + 1069 = 1796 tests.

**Honest limit: this is mitigation, not elimination.** The `dom` project still carries ~330s of environment across 127 files, and one `--project dom` run did still hit the `MainContent.test.jsx` flake (36/36 in isolation). Three greens against a prior 2-in-3 failure rate is strong evidence, not proof. **If it resurfaces, the next lever is `pool`/`poolOptions` concurrency on this 8GB box — NOT raising `testTimeout`,** which would hide the symptom and mask real regressions.

**Two traps found during 024, worth remembering:**
- **Executor worktrees branch from the session's base commit (`d2fba38`), not current HEAD.** The first two attempts verified against a pre-merge tree (181 files) while chasing post-merge numbers (183) that were unreachable there. A plan whose baseline depends on merged work must assert the expected HEAD explicitly — the template's `git diff --stat <SHA>..HEAD` drift check compares backwards and does not catch a wrong base.
- **`npx` emits `npm notice` lines on stderr.** Any `npx vitest list --filesOnly 2>&1 | wc -l` silently inflates the count by one per invocation. Use `2>/dev/null` (or separate the streams) whenever a count is load-bearing.

**023 outcome — recommendation: DELETE, gated on one maintainer check.** Full write-up at `plans/023-outcome-batch-api-decision.md`. The decisive new facts (verified independently by the reviewer with the Grep tool, not shell grep):
- `batch-api` was built in `5d68c6e` for **the app's own frontend**, not an external partner — the origin commit added `batchProcess()`/`processResumeBatch()` to `src/services/api.js` as the intended caller, and all four of that commit's (since-deleted) design docs, recovered via `git show` and read in full, name no outside organisation.
- Commit `114d01f` then **removed both frontend callers while leaving the backend in place** — so this is orphaning-by-refactor, not a deliberate decision to keep a public surface.
- Option C ("make Bulk Analysis use it") is **not** a drop-in rewire: `TaskType` (`batch-api.ts:14`) is only `optimize | predict-questions | generate-cover-letter`, so it cannot run `match`, which is exactly Bulk Analysis's bottleneck. The original five task types included `match` and `parse`; both were stripped out over time. Adopting it means rebuilding removed capability.
- Reviewer re-verified: zero matches for `batch-api|batchProcess|processResumeBatch` anywhere in `src/`.

**The one thing the repo cannot settle:** whether an undocumented external consumer still calls it. Before deleting, pull production `batch-api` invocation logs over 30–90 days. Zero/near-zero confirms; real traffic reopens the decision. Removal is a separate plan and must also retire the `"batch-api"` entry at `rate-limiter.ts:302` and mark `plans/012-rate-limit-batch-api-fanout.md` superseded.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

### Round 3 execution log

Executors ran in isolated git worktrees under `.claude/worktrees/`. The advisor reviewed each diff and re-ran the done criteria; executors never touched `plans/`.

> **MERGED 2026-08-09 at the maintainer's explicit instruction.** All six branches carrying commits (017, 018, 019, 020, 021, 022) were merged into `claude/improve-c68640` with `--no-ff`, so each plan remains a single revertable merge commit. **Zero conflicts** — verified beforehand that no two branches touched the same file and none touched `plans/`. 13 commits, 25 files, +1097/−49. **Not pushed.** Post-merge on the combined tree: `TYPECHECK=0`, `LINT=0`, and **1794 passed / 2 skipped across 183 files** — exactly the 1774 baseline plus the 20 new tests (2+3+3+4+2+6), confirming nothing was lost or double-counted in the merge. To undo everything: `git reset --hard d2fba38`; to drop one plan, revert its merge commit.
>
> **023 was not merged** — its executor hit a quota limit before committing, so it has no commit. Its reviewed deliverable is preserved at `plans/023-outcome-batch-api-decision.md`.
>
> **Plan 024 merged separately as `e0c0a9b`** (2026-08-09), after being written and executed in response to the flakiness the first merge exposed. One file (`vitest.config.ts`), clean merge. The merged tree was verified byte-identical (`git diff HEAD 6f5ae73` empty) to the commit the three green full-suite runs were measured against, so that evidence carries over directly. Final branch state: **`claude/improve-c68640` at `e0c0a9b`, 15 commits ahead of `d2fba38`, not pushed.** Full revert: `git reset --hard d2fba38`; per-plan revert: revert that plan's `--no-ff` merge commit.

| Plan | Branch | Commit | Verdict | Notes |
|------|--------|--------|---------|-------|
| 017 | `advisor/017-restore-add-credits-guards` | `8b5f053` | **APPROVE** | 2 files, both in scope. Reviewer re-ran: `type:check` ok, `lint` clean, `credit-manager.test.js` 34/34. Executor's own full-suite run (pre-handoff) reported 181 files / 1776 passed / 2 skipped / exit 0. Two reviewer-directed corrections during execution: reverted an out-of-scope `.claude/settings.local.json` edit, and added a missing **pre-apply** check to the migration (see deploy gate below). |

| 018 | `advisor/018-rate-limit-credit-endpoints` | `467c57d` | **APPROVE** | 7 files, all in scope. Reviewer re-ran the **full** suite in-worktree: `type:check` ok, `lint` clean, **181 files / 1777 passed / 2 skipped / exit 0** (1774 baseline + exactly 3 new). Documented deviation accepted — see below. |

| 019 | `advisor/019-interview-answer-key-collision` | `2f53d20` + `210b26e` | **APPROVE** (after 1 revision) | All four `savedAnswers` sites keyed by `questionKey` incl. the CSV export; `globalIdx` gone; tests not gamed (CSV test asserts each row holds its own answer AND not the other's). **Revision round 1** — the two `user.type(...)` tests timed out at 10s in the full suite though they passed in isolation (`userEvent`'s default per-keystroke delay). Fixed with `userEvent.setup({ delay: null })` + shorter fixture strings, touching only the test file; no assertion weakened, no timeout raised. Serialised re-run: `EXIT=0`, 182 files, **1777 passed** / 2 skipped (1774 + exactly 3 new). Bonus: after the speed fix the mutation check produces a *clean value mismatch* (`Expected: vuln-a, Received: vuln-astd-b` — the slot accumulating both answers) instead of a timeout, which is a far better demonstration of the bug. |
| 022 | `advisor/022-spike-job-variant-gate` | `41e82fa` | **APPROVE** | 8 files, all in scope; `git diff -- supabase/` **empty** (no Phase-2 work crept in). `TC=0 LINT=0 TEST=0`, 181 files, **1780 passed** / 2 skipped (1774 + exactly 6 new). Three zero-argument events added (`variant_saved`, `variant_opened`, `optimization_completed`) — zero args means PII is structurally impossible, not merely avoided. **Double-count guard verified by reading the diff**: `trackOptimizationCompleted()` sits in the shared success continuation *after* the SSE-vs-legacy branch resolves, not inside either, with two tests proving one call on the SSE path and one — not two — when SSE fails and the legacy fallback succeeds. `variant_opened` fires only from `handleOpen`'s user click and only when `openVariant` returns a real variant; persist-rehydration restores `activeVariantId` without going through that action, so mount cannot inflate the reopen rate. ADR status line corrected. Recommendation doc at `docs/plans/job-variant-phase2-gate.md`. |
| 023 | `advisor/023-spike-batch-api-decision` | **uncommitted** (see note) | **APPROVE** (deliverable) | Deliverable written and reviewed; the executor's session hit a quota limit before committing, and it is now unreachable. The doc is preserved at **`plans/023-outcome-batch-api-decision.md`** in the working tree; the original also sits untracked at `docs/plans/batch-api-decision.md` in worktree `agent-aed7b3443812b8c66`. Zero runtime code touched, as required. |
| 020 | `advisor/020-fail-closed-eval-harnesses` | `47fb5c4` | **APPROVE** | 4 files in scope, no `*.actual.json` touched. Both scripts route default mode through `buildGoldContractOptions` + `unwrapEvaluationResponse` and — critically — cache the **unwrapped** value, so offline re-scoring is not corrupted. Serialised re-run: `EXIT=0`, 181 files, **1778 passed** / 2 skipped (1774 baseline + exactly 4 new). Two notes below. |
| 021 | `advisor/021-server-pdf-detached-buffer` | `e5808f0` | **APPROVE** | 2 files in scope; `src/lib/utils/resumeText.ts` (client copy) untouched. Serialised re-run: `EXIT=0`, **182 files**, **1776 passed** / 2 skipped (1774 + exactly 2 new). **Mutation check is the strongest evidence of the round**: reverting `data: pdfData` → `data: arrayBuffer` reproduced the precise predicted error, `TypeError: Cannot perform Construct on a detached ArrayBuffer` at `netlify/lib/resumeText.js:183` (`arrayBufferToLatin1`), while the happy-path test stayed green. The finding was real and the fix addresses it. Executor improvement on the plan: rather than extending the shared `src/test/__mocks__/pdfjs-dist.mjs` (risking existing client tests), it used per-test `vi.doMock` — an existing pattern in `src/__tests__/resumeText.test.js` — so the shared mock was never touched; and it made the mock genuinely detach via `structuredClone(data, { transfer: [data] })` so the test exercises the real failure mode rather than a plain rejection. |

**⚠ The suite is flaky on this machine — three post-merge runs, measured.** Run 1: 1 failure in `netlify/lib/__tests__/resumeText.test.js`. Run 2: **clean**, 1794 passed. Run 3: 2 failures in `src/__tests__/MainContent.test.jsx`. **A different file each time, every failure a `Test timed out in 10000ms`** — and both files pass in isolation on the merged tree (`resumeText.test.js` 2/2, `MainContent.test.jsx` 36/36, the latter including 022's two new tests, so 022 did not destabilise it). Run 3's headline failure — `"passes structured hard-stop answers to optimization without positive clarification text"` — is the *exact* test already documented as a pre-existing CPU-starvation flake, predating this round. **Conclusion: none of the merged changes are at fault; the merged tree is sound.**

The measured cause is finding #11 in this round's audit. Across the three runs the `tests` phase was stable at 151/155/158s while `environment` ran 519–799s — i.e. happy-dom construction is 3–5× the actual test work, because `vitest.config.ts` applies `environment: "happy-dom"` to all 183 files though 55 (`netlify/`, `eval/`, `scripts/`) are node-only. That load is what pushes borderline tests past the shared 10s `testTimeout`. **The real remedy is the environment split (finding #11), not patching individual tests** — patching whichever test lost the race this run just moves the flake somewhere else, as these three runs demonstrate. Until that lands, treat a single red run as inconclusive and re-run before believing it.

**⚠ Verification methodology — learned the hard way this round.** Do **not** run two full `npm run test` suites concurrently on this machine, and do not trust `npm run test 2>&1 \| tail -N` for pass/fail (the pipe returns `tail`'s exit code, not the suite's). Concurrent runs produced *different* spurious failures in each worktree — `MainContent.test.jsx` onboarding assertions in one, `smoke-functions.test.ts` in the other — all `Test timed out in 10000ms`, i.e. CPU starvation, matching the known flake profile for this 8GB box. Serialise verification runs and capture the summary line + real exit code (`npm run test > file 2>&1; echo $?`). A flaky green is worth no more than a flaky red.

**020 note 1 — accepted regression (reported by the executor, deliberately not fixed):** routing `parse-eval`'s default mode through `disableFallback: true` moves it off `runParser`'s `callOpenRouter` branch (which has a 2-retry transient backoff) onto the `executeAiContract` branch (which has none). A single transient 429/5xx will now zero a fixture in `npm run eval:parse` where it previously retried. `match-eval` is unaffected — its `runMatch` wraps the call in its own retry loop regardless. Accepted because it trades a **silent** wrong answer for a **loud** one: the original bug let a degraded run report a clean pass, whereas a zeroed fixture is visible and re-runnable. Follow-up if it proves annoying: give the `disableFallback` branch of `runParser` the same retry loop, honouring the existing `retryTransient` parameter it currently ignores.

**020 note 2 — one pre-existing assertion changed:** `gold-evaluator-options.test.js:87` asserted `buildGoldContractOptions(...)` returns `{}` in production mode — i.e. it pinned the bug. Updating it was unavoidable and is the exact inverse of the plan's own Step 5 test. No other pre-existing assertion was touched.

**Accepted deviation in 018 (worth knowing before you merge):** the plan assumed the rate limiter fails open in the test environment with no help. It doesn't — `vitest.config.ts` sets `UPSTASH_REDIS_REST_URL`/`_TOKEN` to a fake `test.upstash.io` host, so the limiter would have attempted a real network call instead of taking the "not configured" path. The executor worked around it by `delete`-ing those two env vars at module scope in `feedback-api.test.ts` and `user-data-api.test.ts`, with an explanatory comment, restoring them in `afterEach`. This is a legitimate adaptation and the full suite is green, but note two sharp edges: (a) `originalEnv` is captured *before* the deletes, so `afterEach` puts the vars back mid-file — subsequent tests in those files only stay on the fail-open path because `rate-limiter.ts` memoises its Upstash init at module level; (b) `process.env` is process-global, so the deletes are visible to any file sharing the worker until the first `afterEach` restores them. Green today; fragile if the limiter's init memoisation is ever removed. A cleaner long-term fix is a dedicated test helper that stubs the limiter rather than mutating env.

**Deploy gate for 017 (maintainer action):** the migration adds `check (credits_remaining >= 0)` on `user_credits`. That `alter table` **fails outright** if any existing row is already negative. Before applying, run `select count(*) from public.user_credits where credits_remaining < 0;` — it must return 0. If not, reconcile those rows first; do not drop the constraint. The migration file carries this instruction plus post-apply verification queries against a synthetic email.

## Dependency notes

Round 1:

- 002 requires 001: both edit the same regions of `optimize.ts`/`optimize-stream.ts`; 002's cache-before-credits reorder assumes 001's `success === false` guards exist (an unpaid result must never be cached).
- 003 requires 001+002: the `creditsConsumed` flag brackets the consumption guard from 001, and the safe `cacheOnly` recovery it routes to is only cross-endpoint-reliable once 002 unifies keys.
- 004, 005, 006 are independent of everything else and of each other (005 and 006 both append to `.env.example` — trivial merge).

Round 2 (general improve round):

- **009 first** — it is the verification baseline; every later plan's done criteria get enforced by it.
- 008 requires 007: both edit `credit-manager`-adjacent code and the same test harnesses; 007's RPC-based `addCredits` is the atomicity story 008's reward flow assumes.
- 010 before 013's part C: 013 updates the vision2030 credit-guard test pin that 010 creates (013 parts A/B are independent).
- 011 and 013 both touch optimize-family files (011: optimize.ts/optimize-stream.ts; 013: vision2030 only) — run on separate branches, merge sequentially.
- 015 and 016 both append to `.env.example` — trivial merge.
- **Deploy-order gates (maintainer actions, not executor actions)**: 007 and 016 each produce a migration file the maintainer must run in the Supabase dashboard; both plans' code is written to be safe to deploy before the migration is applied. 016 additionally needs `AI_USAGE_USER_ATTRIBUTION=true` set in Netlify env after the migration.

Security round:

- 007 (sec)–012 (sec) are fully independent of each other and of 001–006; each touches a disjoint file set. No plan depends on another. Recommended order is by priority (008/009/010 first).

Round 3:

- **017–023 are fully independent** — each touches a disjoint file set (017: `supabase/migrations/` + credit tests; 018: three function files + `rate-limiter.ts`; 019: `InterviewSection.tsx`; 020: `scripts/`; 021: `netlify/lib/resumeText.js`; 022: analytics + `JobVariantsBar`; 023: docs only). They can run in parallel on separate branches. Recommended order is by priority: 017, 018, 019, 020, then 021, 022, 023.
- **Deploy-order gate (maintainer action, not executor action)**: 017 produces a migration file that must be run by hand in the Supabase dashboard (`CLAUDE.md`: *"NEVER apply directly"*). The application code is unchanged by 017, so it is safe to deploy in either order.
- **Operator follow-up after 020**: someone with an `OPENROUTER_API_KEY` must run `npm run eval:parse` and `npm run eval:match` once and confirm both still report 8/8 @ 100%. 020 changes which code path `parse-eval` takes to produce its output, so the CLAUDE.md claims about those gates are unverified until that run happens. Executors must NOT run these themselves — they make real, paid AI calls.
- **Operator follow-up after 022**: the ADR's ~4-week measurement window starts when the instrumentation is *deployed*, not merged. The check date must be diarised.

## 2026-07-21 security round — findings & notes

The security-round plans (`007-enforce-cron-secret-and-constant-time-admin.md` … `012-rate-limit-batch-api-fanout.md`) map to these vetted findings:

- **SEC-08** (→ plan 007): admin-secret compared with plain `!==` at `admin-gates.ts:101` while the file already has `timingSafeEqualStrings`. Constant-time swap, P3. (Plan 007 was originally scoped to also cover SEC-07; that was removed — see rejected list.)
- **SEC-40** (→ plan 008): no CSP / X-Frame-Options / HSTS / Permissions-Policy in `netlify.toml`; the app holds a Supabase JWT + resume PII in localStorage with no defense-in-depth. Ships report-only first.
- **SEC-41** (→ plan 009): client Sentry `beforeSend` (`src/main.tsx:29-33`) does no PII scrubbing; only replays are masked.
- **SEC-20** (→ plan 010): the generate-pdf sandbox controls (request interception, protocol allowlist, `setJavaScriptEnabled(false)`) are guarded only by source-string matching; the JS-disable is untested.
- **SEC-21 + SEC-22** (→ plan 011): `safe-fetch.ts:327` lets empty-Content-Type responses past the text-only filter; `job-page-extract.ts:154` runs a ~O(n²) regex on up to 2 MB of attacker-influenced HTML.
- **SEC-09** (→ plan 012): `batch-api` fan-out exported without `withRateLimit` and no own auth — a 1→10 AI amplifier with no top-level throttle.

Vetted but NOT planned (owner's call / lower leverage):

- **SEC-05** (still present, `waitlist-confirm.ts:42-63`): sends a branded email to any well-formed address with no ownership proof; rate-limited but no CAPTCHA. Needs a product decision (CAPTCHA / double-opt-in) before a plan — carried over from the prior round.
- **SEC-06** (still present): raw `error.message` returned to clients on the admin/dev/referral surface — `referral-api.ts:358`, `batch-api.ts:256-267`, `dev-celebration-bonus.ts:147`, `dev-reset-credits.ts:99`, `cron-reset-credits.ts:181`, `cron-monthly-summary.ts:215`, `notify-waitlist.ts:146`. The user-facing AI endpoints (optimize/ai-match/feedback) already sanitize. S effort; a single sweep replacing raw messages with generic text + a logged detail. Not planned only because it's low-severity on already-authenticated/dev surfaces; promote if desired.
- **SEC-42** (dependencies): `npm audit --omit=dev` → 0 critical/high, 4 moderate (dompurify via jspdf — the vulnerable `jspdf.html()` path is not used; opentelemetry via Sentry — telemetry only). Full audit → 2 build-time highs (`brace-expansion`, `shell-quote`) not reachable at runtime. Action: run `npm audit fix` for the build-path highs and re-check whether the `mailparser`/`tar`/`minimatch` overrides are still needed. Hygiene, not urgent — folded into a future toolchain pass rather than its own plan.

Rejected / confirmed clean this round (do not re-audit):

- **SEC-07 (cron gate accepts `x-netlify-internal-functions` header without `CRON_SECRET`) — REJECTED, not exploitable.** The auditor rated it HIGH on code logic but MED on exploitability, flagging "depends on Netlify's inbound-header handling — worth verifying." Verified: Netlify scheduled functions (those with a `schedule =` in `netlify.toml`, i.e. `cron-reset-credits` and `cron-monthly-summary`) **cannot be invoked via a public URL in production** — Netlify docs: *"You can't invoke scheduled functions directly with a URL"* and *"Scheduled functions only run on their schedule for published deploys."* So there is no external HTTP path on which to deliver a spoofed `x-netlify-internal-functions` header; the header-only gate is a safe standard-platform convention, not a vulnerability. **Additional decision-drift note:** plan 005's maintenance note ("remove the header-only back-compat branch once `CRON_SECRET` is confirmed set in production") is **wrong** and must not be acted on — the native Netlify scheduler cannot attach a custom `x-cron-secret` header, so requiring the secret would 403 the real scheduled invocations and silently disable both crons. Plan 007 carries a code comment warning future maintainers off this. Source: https://docs.netlify.com/build/functions/scheduled-functions/
- Money/credit IDOR — none: every credit endpoint verifies the Supabase JWT and derives billing email/id from the verified token, never the request body; `credit-manager.js` keys on the server-derived email.
- CSRF — not applicable: all auth is Bearer-token, no cookie sessions.
- SSRF guard (`safe-fetch.ts`) — DNS-rebinding-safe (connection-time IP validation), redirects re-validated per hop, IPv4-mapped/NAT64/link-local/CGNAT/metadata/TEST-NETs all rejected. Only the empty-Content-Type + regex items (SEC-21/22) remained.
- Puppeteer HTML injection — neutralized by JS-disable + request interception (only the test coverage gap SEC-20 remained).
- Email HTML injection — all user fields pass through `escapeHtml` (plan 005 landed this).
- SQL injection — none; parameterized `.rpc()`/`.eq()` only.
- Prompt-injection-to-sink — none: AI output flows only to escaped React, the JS-disabled Puppeteer renderer, and escapeHtml'd `document.write`; no privileged sink receives raw AI output.
- Hardcoded secrets / secrets in git history — none; only `VITE_WORKER_URL` (public) was ever committed in `.env`. `.env` is gitignored.
- Supabase RLS — owner-scoped on all user tables via `auth.uid() = user_id`; waitlist is insert-only with no anon SELECT.
- CORS `*` without credentials — safe for a bearer-token API (by design).
- Logging/analytics — functions redact via `redactForLog`/`summarizeErrorForLog`; Mixpanel sends only bucketed enums, consent-gated.
- dev-* functions freely callable in prod — false: gated behind BOTH an env allow-flag AND the admin secret (`admin-gates.ts:80-109`).

## 2026-08-08 Round 3 — findings & notes

Planned (→ 017–023):

- **017** — `add_credits` lost BOTH input guards (blank email, non-positive amount) when `20260723000000_fix_credit_transaction_writes.sql:40-60` replaced the version at `20260526_harden_credit_rpcs_and_grants.sql:23-29`. A negative `p_amount` is now an unbounded, unaudited balance subtraction bypassing `consume_user_credits`. Latent (no caller passes a request-controlled amount) but same defect class as the bug that migration existed to fix.
- **018** — `feedback-api.ts:374`, `referral-api.ts:276,386`, `user-data-api.ts:215` export their handlers with **no rate limiting of any kind** (verified: none of the four candidate files contains `withRateLimit`, `checkRateLimitForRequest`, `rateLimit`, or `ratelimit`). Two award credits; one deletes accounts. Same class as SEC-09, which was planned and fixed for `batch-api` — these were missed by that sweep. `rate-limiter.ts:330` also carries a dangling `// Feedback system - INCREASED from 5 to 10` comment with no entry beneath it, which is likely why it went unnoticed.
- **019** — `InterviewSection.tsx` keys `expandedQuestions`/`savedAnswers` off three different index spaces: `questions.indexOf()` at `:718`, the `filteredQuestions` position at `:952`, and a third at `:497-504` for CSV export. With `questions = [V, S1, S2]` (V = vulnerability), V and S1 both use key `0` — one vulnerability question is enough to make typing an answer under one overwrite the other. Destroys user-authored text in a 3-credit feature.
- **020** — Both gold-set eval gates named in `CLAUDE.md` can pass without measuring what they claim. `gold-evaluator-options.mjs:174` returns `{}` for every non-`evaluation` mode so `disableFallback` is never set (silent direct-Gemini fallback), and `:252-256` hardcodes `provider: 'openrouter'` in the report. Separately `parse-eval.mjs:290-293` silently `continue`s past uncached fixtures and `:304-307` exits **0** when nothing scored. `benchmark-ai-models.mjs` already does this correctly and is the exemplar.
- **021** — `netlify/lib/resumeText.js:277-283` hands the raw `arrayBuffer` to `pdfjs.getDocument()` (which detaches it), then `:331` calls `extractPdfTextFallback(arrayBuffer)` on the detached buffer → `new Uint8Array()` throws at `:183`. The client copy fixes this at `src/lib/utils/resumeText.ts:551-556` with a comment naming the exact crash; never ported. Reachability confirmed: `extract-resume-json.ts:198` calls it first on every `kind:"file"` upload, before the OCR branch at `:225`. Effect is degradation, not a 500 — signed-in PDF users get pushed to expensive OCR, guests get `resume/unreadable-file`.
- **Optimize is not tracked at all, in EITHER direction** (upgraded 2026-08-09 during execution of plan 022 — the audit understated this). `trackOptimizationFailed()` is *defined* in `src/services/analytics.ts` but has **no production call site**; its only callers are test mocks (`recommendation-cards.test.jsx`, `optimize-zero-noise.test.jsx`, `optimize-verify-integrity.test.jsx`). And `trackOptimization(action: …)` already carried `'completed'` as a valid but never-invoked union member — a dead earlier attempt. Plan 022 adds the **success** event (the ADR gate's denominator); wiring up the **failure** event was left as a deliberate follow-up. Until that lands, production has zero visibility into optimize failures on the flagship 5-credit action.
- ~~Optimize success is not tracked~~ (superseded by the line above; original audit wording follows). `src/services/analytics.ts` fires `optimization_failed` at `:371` and nothing for a successful optimize run — while **match** has the full `match_analysis_started` / `match_analysis_success` / `match_analysis_failed` triad. Optimize is the flagship 5-credit action; tracking only its failures makes every funnel metric for it uncomputable, and it is why the ADR's save-rate denominator does not exist. Plan 022 adds the missing event.
- **022** (spike) — ADR Phase 1 **shipped** (`jobVariants` slice + `JobVariantsBar`, 9 files incl. tests), but the ADR's own Phase-2 gate (`:224-229`: save rate ≥5%, reopen rate ≥10%) is **unmeasurable** — there is zero variant analytics anywhere in `src/`, and `JobVariantsBar.tsx` imports no analytics module. The ADR's action item 1 (`:253`) targets the now-moot pre-build signal. `ai_usage_events` cannot answer this gate: saving/reopening a variant makes no AI call.
- **023** (spike) — `batch-api` decision, deferred twice already. Re-verified zero callers. Note `batch-api.ts:175-179` reads the auth header but performs no JWT verification of its own, relying entirely on child tasks — relevant if it is ever treated as a third-party surface.

Vetted but NOT planned this round (owner's call / lower leverage):

- **CSP still `Report-Only`** (`netlify.toml:99`). Plan 008 (sec) deliberately shipped report-only and deferred promotion to "a later plan" that was never written. Promoting it is the remaining half of that work. Note the policy's `connect-src` includes `https://unpkg.com` specifically to accommodate the runtime pdfjs cmap/font fetch (see below).
- **Prompt-injection fencing is inconsistent.** Only 2 of 9 system prompts in `netlify/lib/ai-contracts/contracts/index.js` say "treat as untrusted data" — `:1010` (match reality check) and `:1175` (truth check). The main `ai_match` (`:991`), optimize (`:1033`), predict-questions (`:1127`), cover-letter (`:1141`), clarifications (`:1151`) and vision2030 (`:1210`) prompts do not, and imported job-URL page text flows into several of them. The fix pattern already exists in the same file. Bounded impact (no exfil sink, Zod-constrained output), which is why it wasn't planned.
- **Vitest runs `happy-dom` for all 181 test files** though 55 (`netlify/`, `eval/`, `scripts/`) are node-only. Measured profile: `environment 1050s` vs `tests 136s`, 12.5-min wall clock against a 20-min CI cap. Fix is vitest `projects`/`environmentMatchGlobs`. Pure DX, but it protects every future plan's verification gate as the suite grows.
- **CI never runs `npm run build` or `npm run i18n:validate`** (`.github/workflows/ci.yml:21-24`) — a broken production build or a missing Arabic key merges green; Netlify's own build is the only net, and it runs after merge.
- **405/error-envelope drift across 23 functions.** 12 return a bare non-JSON string (`generate-pdf.ts:197`, `ai-match.ts:21`, `user-credits.ts:25`, …), 8 return `{error}`, and only 3 match `CLAUDE.md`'s mandated `{status, code, message}` (`onboard-extract.ts:105`, `import-job-url.ts:103`, `refine-bullet.ts:31`) — each via its own hand-rolled local helper. Plus 17 inline copies of `supabase.auth.getUser(token)` with no shared `requireAuth`. This is the "shared function wrapper" candidate, now unblocked (it was gated on plan 010's tests, which are DONE). M effort; do it as a mechanical per-function migration with each function's existing test as the guard.
- **No test executes migration SQL against real Postgres.** `credit-manager.test.js` mocks `supabase.rpc`, so it can only assert what the JS *assumes* the RPC does — which is exactly why finding 017 was invisible. The ledger's integrity lives in four `SECURITY DEFINER` functions with zero executable coverage, and the repo has now shipped three silent breakages in that area (the `feature` NOT NULL gap, the `transaction_type` CHECK gap, and 017's guard drop), all found manually after the fact. M effort.
- **Credit-charged AI calls have no abort/unmount guard.** `Vision2030Section.tsx:136-202` (2 credits) and `InterviewSection.tsx:361-424` (3 credits) both await across a tab switch that unmounts them; `api.js:158-187`'s `retryWithBackoff(fn, 3, 2000)` widens the window to many seconds. Vision2030 can land on a 60s stale-timeout that clears to the initial prompt even though a valid paid-for result sits in `localStorage`.
- **Zero tests** on `src/lib/optimize/appliedSubsetVerification.ts` (a pure function gating a paid call — the cleanest test target in the codebase) and `src/components/sections/InterviewSection.tsx` (1134 lines; plan 019 creates the first test file for it).
- **Runtime dependency on unpkg.com on the core upload path.** Both `src/lib/utils/resumeText.ts:565,567` and `netlify/lib/resumeText.js:280,282` fetch pdfjs cmaps and standard fonts from unpkg at parse time. Self-hosting them would remove a third-party availability dependency from the main funnel and let the CSP drop `https://unpkg.com` from `connect-src`. Build/deploy change, not a bug fix. (Plan 021 fixes only the *stale hardcoded version* half of this.)
- **Docs drift**: `CLAUDE.md:101` says "25 TypeScript functions" — there are 26 (`user-credits.ts` is missing from the inventory, on the money-adjacent path). The ADR status line is stale (plan 022 fixes that one). `plans/README.md:120`'s "import-job-url → Pipeline auto-fill" candidate **shipped** in `3649873` — corrected below.
- **No `.nvmrc` and no `engines` field** despite `netlify.toml:21` and CI both pinning Node 20 and `README.md:112` stating it as a requirement. Nothing enforces or auto-switches locally.
- **Dead manifest entries**: the `mailparser` override (`package.json:110`) resolves to nothing — `npm ls mailparser --all` is empty and it is absent from the lockfile; and `tailwindcss-animate` (`package.json:67`) is declared but never imported (only `tw-animate-css` is, at `src/index.css:3`; there is no `tailwind.config.*` and no `@plugin` directive). The `tar` and `minimatch` overrides ARE still needed — verified resolving.
- **Undocumented version coupling** between `puppeteer-core` and `@sparticuz/chromium` on the `generate-pdf` path. The vendor's own README requires matching the Chromium build to the Puppeteer major; nothing in the repo records this, and the existing PDF tests mock at module load so they would not catch a real binary/protocol mismatch. A one-line `CLAUDE.md` gotcha would close it.
- **Small cluster, S each**: two stale `it.skip` tests at `OptimizeSection.test.jsx:924,967` asserting a `fetch` the component no longer makes (the call moved to `MainContent.tsx`) · dead `[functions."ai"]` block at `netlify.toml:39-40` for a function that does not exist · `optimize.ts:236` detects a shared-lib failure by matching `error.message` text against the literal thrown at `optimize-cards.ts:169` · `user-credits.ts:34,84` uses `{error}` rather than the mandated envelope · `TemplatesSection.tsx:271-275` omits `isSaudiNational` from a `useMemo` dep array its own comment says it needs (latent — not reachable today, the checkbox lives under a mutually-exclusive tab) · `UploadSection.tsx:53` is the only `useResumeStore()` call in `src/` with no selector · `vision2030Skills` (~45KB) eagerly imported at `OptimizeSection.tsx:22` for a page-refresh recovery branch at `:372-375` · `.env.example` missing `EVAL_THRESHOLD`, `SMOKE_BASE_URL`, `WATHEQ_EVAL_JUDGE_MODEL`, `ANALYZE`/`ANALYZE_JSON` (all have safe inline defaults).

**PRICING — decided this round, do not re-raise.** Six endpoints call AI and charge no credits: `refine-bullet`, `resume-truth-check`, `extract-job-metadata`, `generate-clarifications`, `onboard-extract`, `import-job-url`. The owner confirmed on 2026-08-08 that this is **deliberate** — they stay free. Note they mostly also fall through to `ENDPOINT_RATE_LIMITS.default` (30/min); if abuse cost ever becomes visible, tighten the limits rather than reopening the pricing question.

## Findings considered and rejected (round 3 — so nobody re-audits them)

- **`d2fba38` deleting `src/__tests__/bug-score-display.test.tsx` — NOT a weakened test.** The deleted file was a brittle *source-regex* test asserting `OptimizeSection.tsx` contained a specific assignment string. That logic legitimately moved to `MainContent.tsx:278-286` / `:1534-1558`, and the behaviour is now covered by real behavioural assertions at `MainContent.test.jsx:1139-1196` (store receives `afterScore: 80` on both the SSE and legacy-fallback paths). Strictly better coverage; the deletion was correct.
- **`optimize-stream` missing `withRateLimit` — false alarm.** It is a Netlify Functions v2 handler and uses the v2 equivalent, `checkRateLimitForRequest(request, "optimize-stream")` at `optimize-stream.ts:114`. Properly limited.
- **`optimize-cards.ts` extraction — clean.** Verified byte-faithful against the pre-extraction `optimize.ts`/`optimize-stream.ts`; no guard was dropped in the refactor.
- **`user-credits.ts` — clean on identity and caching.** Derives identity solely from `client.auth.getUser(token)`; never reads it from the request body. No server-side balance cache (the only cache is a 1s client dedupe in `CreditsContext.tsx`, backstopped by RLS). No IDOR.
- **`initial_grant` migration ordering — downgraded, folded into 017.** `20260721000000` writes a `transaction_type` only whitelisted by `20260723000000`, but its own comment at `:76-77` instructs applying both together, and `CLAUDE.md` says migrations are hand-run in the dashboard. Structurally backwards, no real-world exposure. 017's forward migration makes the CHECK idempotent so a fresh rebuild is safe.
- **`add_credits` guard corroboration via `credit-manager.test.js:818-828` — the reasoning is wrong, though the finding stands.** That test mocks SQLSTATE `23514` (a CHECK violation), but a plpgsql `raise exception` yields `P0001`. The test never pinned the DB guard at all. Finding 017 rests on the SQL diff alone. Do not repeat the test claim.
- **Round 1/2 rejections still stand** (SEC-07 cron gate, `safe-fetch` SSRF, feature flags by design, `parse-quality.js` size, secrets/git history, CORS, RLS, SQL injection, dev-* gating). Re-confirmed where touched.
- **`@supabase/supabase-js` pin at `2.104.1`** — verified only one resolved copy in the lockfile; nothing transitively wants a newer version. Pin is correct and unthreatened.
- **`statistics.mjs`** — median / nearest-rank p95 / mean all computed correctly; division-by-zero structurally impossible (`assertLatencySamples` rejects `count === 0` first); n=1 and n=2 behave sanely. Eval stage-gates fail closed on empty sets (`matrix.mjs:171-192`). Fixture manifest ↔ disk consistency is enforced by `eval/__tests__/model-eval-fixture-manifest.test.js`.
- **`docs/SUPABASE_SCHEMA_DRIFT_20260522.md`, `docs/AI_MODEL_BENCHMARKING.md`, `docs/onboarding-v1.md`, `docs/plans/trust-pair-plan.md`, `docs/plans/first-customer-insights-plan.md`** — all read and cross-checked against code; accurate, not stale.
- **`puppeteer` (full) vs `puppeteer-core`** — deliberate split (full only in dev scripts, core + `@sparticuz/chromium` in `generate-pdf`), not a duplicate dependency.
- **`aggregateGoldAttempts([])` reporting a vacuous pass** — real inconsistency with its fail-closed siblings, but unreachable from all four current call sites. Folded into plan 020 as a one-line guard rather than planned separately.
- **Test-suite integrity, repo-wide** — no `.only`, no `.todo`, no `xit`/`xdescribe`, no commented-out `expect`, no `expect(true)` tautologies; every `toBeDefined()` sampled had a real companion assertion. All committed test files fall inside the vitest include globs — nothing silently never runs. Only the two stale `it.skip` above.
- **No prompt-injection content** was found in any file read across all four auditors and the direct pass.

## Candidates not planned this round (promote or reject next round)

- **DEBT: shared function wrapper** — **UNBLOCKED** as of round 3: the gate ("do AFTER 010's tests exist") is satisfied, plan 010 is DONE. Round 3 re-measured the drift precisely — 23 functions with method guards, 4 different 405 envelope shapes, only 3 compliant with `CLAUDE.md`, 17 inline `auth.getUser` copies. See the round-3 findings section above for the full file:line evidence. M. Promote next round.
- **DEBT: god components** — `MainContent.tsx` 2432 lines (23 commits since June), `OptimizeSection.tsx` 1655. Incremental leaf-panel extraction, characterization tests first; easier after 012's selectors map state usage. L.
- **DEBT: untyped services** — `src/services/api.js` (1131 lines) and `exportPdf.js` (869) still `.js`; convert exportPdf first to validate approach. L. (`referrals.ts` error handling — previously flagged — is fixed and tested.)
- **DEBT: rate-limiter split** — move generic `withRetry`/`batchWithConcurrency` out of `rate-limiter.ts` (760 lines); consolidate the four `check*` variants in a second, test-guarded step. M.
- **SEC: waitlist-confirm ownership proof** — still sends branded email to any address (rate-limit only). Needs a product decision (insert-gated send vs signed token vs CAPTCHA). S–M.
- **SEC: prompt-injection fencing for imported job pages** — still open, and round 3 sharpened it from MED to HIGH confidence: the fix pattern **already exists in the same file** but is applied to only 2 of 9 system prompts (`contracts/index.js:1010`, `:1175`). See the round-3 findings section for the full list of prompts missing it. Bounded impact keeps it unplanned. M.
- ~~**PRICING decision**~~ — **DECIDED 2026-08-08: deliberate, they stay free.** See the round-3 PRICING note above. Do not re-raise.
- **DECISION: `.agents/skills/` vs `.claude/skills/`** — both tracked, diverged (each has files the other lacks). Pick canonical or document why both.
- ~~**DECISION: `batch-api` surface**~~ — **now plan `023-spike-decide-batch-api-surface.md`** (deferred twice; round 3 turned it into a decision spike). Re-verified zero callers.
- **Latent: unverified-email rows stuck at 0 credits** — legacy rows created by the (dead) unverified branch have no re-grant path; 007 notes it, nothing re-grants after verification.
- **Direction (unselected as of round 3)**:
  - **Moyasar credit pack** — still open, re-verified 2026-08-08: complete Phase-1 plan at `.claude/plans/moyasar-payment-gateway-plan.md`, and **zero** implementation (no `moyasar`/`create-purchase`/`purchase-catalog` matches anywhere in `src/`, `netlify/`, `supabase/`). `PricingSection.tsx:21-23` still routes the paid plan to `PricingWaitlistModal`. L, and gated on merchant-account approval (non-code critical path).
  - **Email nudges** — Resend + templates + cron infra all built; only callers are the two crons and the two transactional waitlist functions. No lifecycle/re-engagement sends despite the triggering signals (guest run counters, saved-but-unopened variants, quiet Pipeline entries) already existing. M. Brand-sensitive: must not read as growth-hacky against the score-honesty posture.
  - **Job Variants ↔ Pipeline linkage** — `JobVariant` (`src/types/templates.ts`, local/Zustand) and `JobApplication` (`src/types/pipeline.ts`, Supabase) both model "one job" and neither references the other, so a user re-enters company/title twice with no way to move between tailored resume and application status. The ADR's own §8 Phase 3 describes materially what Pipeline already is. M. **New in round 3.**
  - **Progression profile** — `src/lib/progression/careerLevels.ts:9-10` states in code that nothing is persisted, by design. Persisting it is a cheap extension of shipped machinery but sits in direct tension with the anti-inflation/score-honesty posture. Needs a brand decision *before* any build, not after. M.
  - ~~import-job-url → Pipeline auto-fill~~ — **SHIPPED** in `3649873`. `MainContent.tsx:1180-1206` merges verbatim LinkedIn `importedCriteria` over AI-guessed `extractJobMetadata` output, feeding `pipelineAutoSave.ts`'s `shouldAutoSaveJob` and `SaveJobToPipelineCard.tsx`. Removed from candidates so a future round does not re-propose it.

## Findings considered and rejected (round 2 — so nobody re-audits them)

- `safe-fetch.ts` SSRF concerns — verified clean by direct read: connection-time DNS validation, per-hop redirect re-validation, IP-literal canonicalization, byte/deadline caps. Tested.
- `parse-quality.js` as junk drawer — cohesive around deterministic parse recovery, has dedicated tests; size is a watch item only.
- Progression (`src/lib/progression`) and feature-flag runtime untested — false; both have test files.
- Feature flags "never rolled out" — by design: registry documents them as dev-only kill switches, all default-on, "not a rollout mechanism".
- `npm audit`: 4 moderate advisories (`@opentelemetry/core`, `dompurify` transitive), zero high/critical on reachable paths — below reporting threshold.
- Dependency duplication/lag — none with real cost (re-checked).
- Round-1 rejections still stand (see git history of this file for the full list: batch-parse endpoint claim, money-path libs untested, tests-hit-network, log-litter, `parse-resume` auth, snapshot tests, documented CLAUDE.md tradeoffs).

## Stale-knowledge corrections found during this audit

- Agent memory claiming "prod fully tree-shakes flag runtime" is stale — `src/hooks/useFeatureFlag.ts:5-13` documents `APPLY_OVERRIDES = true` in dev AND prod as a deliberate tradeoff.
- The prior audit's direction list assumed `docs/WATHEQ_ENGINEERING_PLAN.md` exists — deleted in `dee2ee3`; plan 015 repairs the ADR references to it.
