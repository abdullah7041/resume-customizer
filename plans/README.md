# Implementation Plans

Generated by the improve skill on 2026-07-08 against commit `baebbd7` (branch `codex/optimize-page-pass1-pass2`; plans target `main`-based branches). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Selection note: this audit ran non-interactively, so the advisor wrote plans for the top findings by leverage (impact ÷ effort, weighted by confidence) as the default set. The full findings table, including items deliberately left unplanned, is below so the owner can promote or reject items.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Enforce credit consumption results in optimize endpoints | P1 | S | — | DONE |
| 002 | Unify optimize cache key; stop serving cached credit balances | P1 | M | 001 | DONE |
| 003 | Billing-safe stream error events (no double-charge fallback) | P1 | S–M | 001, 002 | DONE |
| 004 | Atomic referral completion (claim-then-award) | P2 | S | — | DONE |
| 005 | Harden abuse-facing surfaces (cron gate, beta gate, dead fn, email escaping) | P2 | M | — | DONE |
| 006 | Toolchain & repo hygiene (dead dep, tracked artifacts, honest gate, env docs) | P2 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 requires 001: both edit the same regions of `optimize.ts`/`optimize-stream.ts`; 002's cache-before-credits reorder assumes 001's `success === false` guards exist (an unpaid result must never be cached).
- 003 requires 001+002: the `creditsConsumed` flag brackets the consumption guard from 001, and the safe `cacheOnly` recovery it routes to is only cross-endpoint-reliable once 002 unifies keys.
- 004, 005, 006 are independent of everything else and of each other (005 and 006 both append to `.env.example` — trivial merge).

## Vetted findings not planned (candidates for a next round)

- **PERF-01** `OptimizeSection.tsx:156-171` subscribes to the whole Zustand store (no selector) — every store write re-renders the 1554-line component during streaming. Fix: per-field selectors / `useShallow`. M effort.
- **PERF-02** `src/services/analytics.ts:1` statically imports mixpanel-browser and `src/main.tsx:46` inits it at boot, before/regardless of consent. Fix: dynamic import behind consent. S–M.
- **PERF-03** `src/index.css` (63 KB hand-written) ships dead CSS incl. 4 `hr-super-saud` keyframes for a feature hard-disabled in `App.tsx:30`, and `@keyframes float` defined twice (lines 1485, 2542). M, needs visual verification.
- **DEBT-03** ~110 lines of card-building logic duplicated between `optimize.ts:210-321` (inline) and `optimize-stream.ts` (`buildOptimizationCards`) — extract to `netlify/lib/`. Best done right after 002/003 land. M.
- **DEBT-04/05** No shared fetch wrapper in `src/services/` (referrals.ts has zero error handling); high-traffic `api.js` (1076 lines) and `exportPdf.js` are untyped `.js` in a TS repo. M–L.
- **DEBT-06** `rate-limiter.ts` (756 lines) mixes generic `withRetry`/`batchWithConcurrency` utils with six overlapping rate-limit checks. M.
- **DEBT-07** God components: `MainContent.tsx` 2248 lines, `OptimizeSection.tsx` 1554 (repo median 136). Incremental decomposition. L.
- **SEC-05** `waitlist-confirm.ts` sends branded email to any address with no ownership proof (rate limiter is fail-open). S, needs product decision on CAPTCHA.
- **SEC-06** Several endpoints return raw `error.message` to clients (`batch-api.ts:275`, `referral-api.ts:266`, dev-* functions). S.
- **SEC-07** Full resume PII persists in localStorage across sessions (`watheq:resumeData`) — largely by-design for guest continuity; decide + document retention, clear on sign-out. Investigate-and-decide.
- **DX (minor)** `batch-api.ts` maps `parse` → legacy `parse-resume.ts`; the whole batch surface has zero frontend callers. Owner decision: keep for beta/API users (then plan 005's gate matters) or delete the surface.

## Direction findings (owner's call, grounded in repo evidence — not ranked against bugs)

1. **Day 1/3/7 email nudges** — the entire stack already exists (Resend + `email-service.js` + 56 KB of templates + two working scheduled functions incl. a monthly summary). A "you optimized but haven't applied" nudge is one more cron + one template. Strongest adjacent-possible in the repo; named P2 in the retention roadmap. Coarse effort: days.
2. **Resume vault (multi-version)** — a Supabase `resumes` table already exists with export/delete in `user-data-api.ts` and `search_intent` per-row updates; the roadmap names re-upload friction the #1 retention killer. Missing piece is list/load UI + version-per-job association. Coarse effort: ~1–2 weeks.
3. **Live score updates while editing** — the auto-verify machinery (temp apply/revert via `useResumeStore.getState()`) already computes score deltas post-optimization; wiring a debounced re-score into edit flows is the roadmap's "#1 engagement hook". Coarse effort: ~1 week, plus AI-call cost per re-score to design around (cache/deterministic pre-score).
4. **Bilingual AR+EN export** — RTL rendering, direction-aware templates, and Arabic localization are built; the roadmap calls one-click bilingual export a differentiator no competitor does well. The dead `parse-arabic-resume.ts` (deleted in plan 005) is unrelated to this — export is template-side. Coarse effort: 1–2 weeks incl. font/layout QA.

Also noted: the engineering plan's own "next task" (production analytics traffic smoke) requires Mixpanel/Netlify dashboard access, not code — it can't be a plan here.

## Findings considered and rejected

- "Batch `parse` maps to a nonexistent endpoint" — wrong: `parse-resume.ts` exists; the real issues (stale mapping, presence-only gate) are in plan 005.
- "Money-path libs untested" — false: `credit-manager`, `referral-manager`, `rate-limiter`, `parse-quality`, `openrouter-client` all have real adjacent tests (deduction races, fail-closed, RPC fallback).
- "Tests hit real network" — no evidence; all AI-touching function tests mock the client boundary. (The known flaky OCR test under `quality:parallel` is an env-var issue in parallel runs, not a missing mock.)
- "Log-file litter needs gitignore work" — `*.log` already ignored; nothing tracked.
- Dependency major-version lag — none with real cost (TS 6, Zod 4, Vite 8, React 19 all current). Puppeteer/`puppeteer-core`/`@sparticuz/chromium` split is a coherent local-vs-serverless pair.
- `parse-resume.ts` lacking auth as a security hole — unauthenticated parsing is by-design (guest preview path in `extract-resume-json.ts:58-88`); the residual question is dead-surface removal, tracked above.
- Snapshot/brittle UI tests — checked: the two biggest suites are behavior-based RTL assertions, zero snapshots/innerHTML.
- Documented tradeoffs from CLAUDE.md (json_object parse format, reasoningBudget 0, fail-open rate limiter, Sentry chunking, etc.) — settled decisions, excluded throughout.
