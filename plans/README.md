# Implementation Plans

Round 2 generated by the improve skill on **2026-07-21** against commit `ceed480` (branch `main`). Round 1 (2026-07-08, commit `baebbd7`) produced plans 001–006 — all verified landed on 2026-07-21 with no regressions (optimize billing guarantees re-checked after #123; `safe-fetch` SSRF guard re-verified clean; root artifacts untracked; `parse-arabic-resume.ts` deleted).

Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Selection note (round 2): the owner selected all four offered clusters plus the usage-attribution direction spike. Unselected findings are recorded under "candidates" below.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Enforce credit consumption results in optimize endpoints | P1 | S | — | DONE |
| 002 | Unify optimize cache key; stop serving cached credit balances | P1 | M | 001 | DONE |
| 003 | Billing-safe stream error events (no double-charge fallback) | P1 | S–M | 001, 002 | DONE |
| 004 | Atomic referral completion (claim-then-award) | P2 | S | — | DONE |
| 005 | Harden abuse-facing surfaces (cron gate, beta gate, dead fn, email escaping) | P2 | M | — | DONE |
| 006 | Toolchain & repo hygiene (dead dep, tracked artifacts, honest gate, env docs) | P2 | S | — | DONE |
| 007 | Restore signup anti-abuse checks; atomic addCredits | P1 | M | — | DONE |
| 008 | Pay referral rewards on first paid action | P1 | M | 007 | DONE |
| 009 | CI pipeline gating main (lint/typecheck/test) | P1 | S | — | DONE |
| 010 | Handler tests for six untested AI functions | P2 | M | 009 (soft) | DONE |
| 011 | Extract shared optimize card/score lib | P2 | M | — | DONE |
| 012 | OptimizeSection render hygiene (selectors/useCallback/memo) | P2 | M | — | DONE |
| 013 | Mixpanel deferral + client error-envelope hygiene + vision2030 credit guard | P2 | S–M | 010 (part C only) | DONE |
| 014 | Gate parked mascot CSS; right-size character images | P3 | S | — | DONE (B2 skipped: no approved local image encoder) |
| 015 | Docs & DX hygiene (README setup, env docs, CLAUDE.md de-drift, spec archive) | P3 | S | — | DONE |
| 016 | SPIKE: user attribution on ai_usage_events (unblocks job-variant ADR) | P2 | S–M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **009 first** — it is the verification baseline; every later plan's done criteria get enforced by it.
- 008 requires 007: both edit `credit-manager`-adjacent code and the same test harnesses; 007's RPC-based `addCredits` is the atomicity story 008's reward flow assumes.
- 010 before 013's part C: 013 updates the vision2030 credit-guard test pin that 010 creates (013 parts A/B are independent).
- 011 and 013 both touch optimize-family files (011: optimize.ts/optimize-stream.ts; 013: vision2030 only) — run on separate branches, merge sequentially.
- 015 and 016 both append to `.env.example` — trivial merge.
- **Deploy-order gates (maintainer actions, not executor actions)**: 007 and 016 each produce a migration file the maintainer must run in the Supabase dashboard; both plans' code is written to be safe to deploy before the migration is applied. 016 additionally needs `AI_USAGE_USER_ATTRIBUTION=true` set in Netlify env after the migration.

## Candidates not planned this round (promote or reject next round)

- **DEBT: shared function wrapper** — 8 new functions copy-paste auth/method/parse/error boilerplate with already-diverged envelopes (`extract-job-metadata.ts:33` bare-string 405 vs structured envelopes elsewhere). Do AFTER 010's tests exist. M.
- **DEBT: god components** — `MainContent.tsx` 2432 lines (23 commits since June), `OptimizeSection.tsx` 1655. Incremental leaf-panel extraction, characterization tests first; easier after 012's selectors map state usage. L.
- **DEBT: untyped services** — `src/services/api.js` (1131 lines) and `exportPdf.js` (869) still `.js`; convert exportPdf first to validate approach. L. (`referrals.ts` error handling — previously flagged — is fixed and tested.)
- **DEBT: rate-limiter split** — move generic `withRetry`/`batchWithConcurrency` out of `rate-limiter.ts` (760 lines); consolidate the four `check*` variants in a second, test-guarded step. M.
- **SEC: waitlist-confirm ownership proof** — still sends branded email to any address (rate-limit only). Needs a product decision (insert-gated send vs signed token vs CAPTCHA). S–M.
- **SEC: prompt-injection fencing for imported job pages** — fetched page text (incl. hidden HTML) flows into optimize/match prompts unfenced; bounded impact (no exfil sink; Zod-constrained output). Fix = untrusted-data markers in prompts + strip non-visible content in `job-page-extract.ts`. M, MED confidence.
- **PRICING decision**: `refine-bullet`, `resume-truth-check`, `extract-job-metadata`, `generate-clarifications` charge no credits (refine-bullet's header suggests deliberate). Confirm intent; add `FEATURE_COSTS` entries if not.
- **DECISION: `.agents/skills/` vs `.claude/skills/`** — both tracked, diverged (each has files the other lacks). Pick canonical or document why both.
- **DECISION: `batch-api` surface** — zero frontend callers; Bulk Analysis uses its own path. Keep for API users or delete.
- **Latent: unverified-email rows stuck at 0 credits** — legacy rows created by the (dead) unverified branch have no re-grant path; 007 notes it, nothing re-grants after verification.
- **Direction (unselected this round)**: Moyasar credit pack (complete Phase-1 plan at `.claude/plans/moyasar-payment-gateway-plan.md`; funnel exists, gateway absent; L); email nudges (Resend + 57KB templates + cron infra idle beyond monthly summary; M); import-job-url → Pipeline auto-fill (extracted fields match Pipeline card needs; also the on-ramp to the variant ADR; M); progression profile from #122 companion (level-up machinery unused across sessions; brand tension with score-honesty posture; MED confidence).

## Findings considered and rejected (round 2 — so nobody re-audits them)

- `safe-fetch.ts` SSRF concerns — verified clean by direct read: connection-time DNS validation, per-hop redirect re-validation, IP-literal canonicalization, byte/deadline caps. Tested.
- `parse-quality.js` as junk drawer — cohesive around deterministic parse recovery, has dedicated tests; size is a watch item only.
- Progression (`src/lib/progression`) and feature-flag runtime untested — false; both have test files.
- Feature flags "never rolled out" — by design: registry documents them as dev-only kill switches, all default-on, "not a rollout mechanism".
- `npm audit`: 4 moderate advisories (`@opentelemetry/core`, `dompurify` transitive), zero high/critical on reachable paths — below reporting threshold.
- Dependency duplication/lag — none with real cost (re-checked).
- Round-1 rejections still stand (see git history of this file for the full list: batch-parse endpoint claim, money-path libs untested, tests-hit-network, log-litter, `parse-resume` auth, snapshot tests, documented CLAUDE.md tradeoffs).

## Stale-knowledge corrections found during this audit

- Agent memory claiming "prod fully tree-shakes flag runtime" is stale — `src/hooks/useFeatureFlag.ts:5-13` documents `APPLY_OVERRIDES = true` in dev AND prod as a deliberate tradeoff.
- The prior audit's direction list assumed `docs/WATHEQ_ENGINEERING_PLAN.md` exists — deleted in `dee2ee3`; plan 015 repairs the ADR references to it.
